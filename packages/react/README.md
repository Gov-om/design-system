# GUP React components

Components for React &lt;19 generated automatically from Web components contained in the @gup-ds/components package.

Note you probably don't need to utilise this package if using React 19 because it finally provides full support for Web components.
