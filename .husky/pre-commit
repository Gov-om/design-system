#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

npx nx run @govom/components:lint:css && npx nx run @govom/components:lint:prettier && npx nx run @govom/components:lint:eslint && npx nx run @govom/components:lint:ts
